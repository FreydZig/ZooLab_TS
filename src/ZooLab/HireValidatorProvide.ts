import {IEmployee} from "./Interfaces/IEmployee";
import {IHireValidator} from "./Interfaces/IHireValidator";

export class HireValidatorProvide{
    GetHireValidator(employee: IEmployee): IHireValidator{
        return;
    }
}