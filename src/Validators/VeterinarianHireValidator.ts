import {IHireValidator} from "../Interfaces/IHireValidator";
import {IEmployee} from "../Interfaces/IEmployee";

export class VeterinarianHireValidator implements IHireValidator{
    ValidateEmployee(employee: IEmployee): Array<string> {
        //TODO: Add logic Validation
        return undefined;
    }

}